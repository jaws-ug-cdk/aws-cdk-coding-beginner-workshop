+++
title = "はじめに"
chapter = true
weight = 10
+++

<!--
  章扉（chapter = true）のサンプル。
  front matter は TOML 形式（+++ で囲む）でも YAML 形式（--- で囲む）でも書けます。
  この章では「このワークショップは何か」「何を作るか」を説明します。
-->

# はじめに

この章はサンプルです。ワークショップの背景・全体像・完成イメージを書きます。

たとえば次のような内容を入れます。

- AWS CDK とは何か（1〜2 段落で簡潔に）
- このワークショップで最終的に作るもの（アーキテクチャ図など）
- 想定する所要時間と前提知識

{{% notice tip %}}
画像を載せるときは、この `_index.md` と同じディレクトリに画像ファイルを置き、
`![代替テキスト](画像ファイル名.png)` で参照します（ページバンドル方式）。
{{% /notice %}}
